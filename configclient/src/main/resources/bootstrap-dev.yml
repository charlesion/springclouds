spring:
  application:
    name: configclient
  cloud:
    config:
      uri:  http://localhost:8888/

server:
  port: 8881

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
